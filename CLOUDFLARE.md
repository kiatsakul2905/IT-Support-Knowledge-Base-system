# Cloudflare Pages Configuration
# For Cloudflare Pages, use @cloudflare/next-on-pages

# Install: npm install -D @cloudflare/next-on-pages

# Build command in Cloudflare Pages dashboard:
# npx @cloudflare/next-on-pages

# Build output directory: .vercel/output/static

# Required Environment Variables in Cloudflare Pages:
# DATABASE_URL
# ADMIN_PASSWORD
# SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS, SMTP_FROM
# NEXT_PUBLIC_APP_URL

# NOTE: For full Next.js features with Server Components + Route Handlers,
# Vercel is recommended. Cloudflare Pages supports Next.js via next-on-pages adapter.
